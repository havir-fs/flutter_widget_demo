import 'package:flutter/material.dart';

/// This class is to demonstrate how to use TextButton, ElevatedButton,
/// OutlinedButton, and Switch widget. In addition, Stateless vs Stateful.
class Buttons extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text('Flutter Buttons'),
        ),
        body: Container(),
      ),
    );
  }
}
